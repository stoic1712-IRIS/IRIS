{
  "schema_version": "1.0.0",
  "handoff_id": "verification-preflight-ci-and-contract-source-guard",
  "task_id": "verification-preflight-ci-and-contract-source-guard",
  "producer": "Claude",
  "recipient": "Founder Cristofer Stoic Arellano as independent reviewer",
  "created_at": "2026-08-12T12:28:17Z",
  "base_revisions": [
    {
      "repository": "stoic1712-IRIS/IRIS",
      "branch": "main",
      "commit": "08f69f82846e40d1a428f4238da5f14918965fa1"
    }
  ],
  "result_revisions": [
    {
      "repository": "stoic1712-IRIS/IRIS",
      "branch": "claude/remote-control-v1owqp",
      "commit": "77f18f2bd65714f71a3902bcb06e6a1f1683dfe8",
      "patch_digest": null
    }
  ],
  "changed_paths": [
    ".iris/coordination/tasks/verification-preflight-ci-and-contract-source-guard.json",
    ".iris/coordination/handoffs/verification-preflight-ci-and-contract-source-guard.json",
    ".github/workflows/verify.yml",
    ".claude/settings.json",
    "scripts/hooks/contract-bound-source-guard.mjs",
    "docs/operations/verification-preflight.md",
    "evidence/verification-preflight/ci-and-contract-source-guard.md"
  ],
  "commands": [
    {
      "repository": "stoic1712-IRIS/IRIS",
      "command": "pnpm install --frozen-lockfile --ignore-scripts",
      "exit_code": 0
    },
    {
      "repository": "stoic1712-IRIS/IRIS",
      "command": "guard behavior matrix: 8 payloads piped to scripts/hooks/contract-bound-source-guard.mjs",
      "exit_code": 0
    },
    {
      "repository": "stoic1712-IRIS/IRIS",
      "command": "node -e \"byte comparison of .claude/settings.json permissions against main\"",
      "exit_code": 0
    },
    {
      "repository": "stoic1712-IRIS/IRIS",
      "command": "pnpm diagnostics",
      "exit_code": 0
    },
    {
      "repository": "stoic1712-IRIS/IRIS",
      "command": "pnpm exec prettier --write scripts/hooks/contract-bound-source-guard.mjs",
      "exit_code": 0
    },
    {
      "repository": "stoic1712-IRIS/IRIS",
      "command": "pnpm exec vitest run --exclude tests/founder-windows-startup.test.ts --exclude tests/iris-dev-github.test.ts",
      "exit_code": 0
    },
    {
      "repository": "stoic1712-IRIS/IRIS",
      "command": "pnpm verify (2 pre-existing environmental test failures remain, identical at clean main)",
      "exit_code": 1
    }
  ],
  "evidence": [
    "The guard returns an ask decision for a digest-bound path by relative and absolute form, naming role and pinned digest, and stays silent for ordinary paths.",
    "The discriminating case passes: docs/registries/dependency-attribution-registry.md is not contract-bound and the guard stays silent, so it matches the exact bound source list rather than a directory.",
    "The guard fails open on malformed JSON, absent file_path, empty stdin, and a path outside the repository; all four exit 0 with no decision and no blocking behavior.",
    "The guard reads the bound source list from config/iris-operating-contract.v1.json at runtime, so it needs no update when the eleven-source set changes.",
    "Byte comparison against main proves permissions.allow (24), permissions.ask (36), and permissions.deny (65) are identical, defaultMode, disableBypassPermissionsMode, and autoMemoryEnabled are identical, the only key added is hooks, and no key was removed.",
    "The workflow declares permissions contents: read, sets persist-credentials false, references no secret or variable, uses only first-party actions/checkout@v4 and actions/setup-node@v4, cancels superseded runs, and carries a 20-minute timeout.",
    "The CI-equivalent test command is fully green: 63 files, 524 passed, 1 pre-existing skip, exit 0.",
    "The compiled operating contract digest is unchanged at sha256:9ba317acac51f3592fb16db0f7c1beef49b867eb5759f5803230964753b1327a, confirming no digest-bound source was touched by this task.",
    "The existing wave-10-resource-proof.yml workflow is unmodified.",
    "pnpm diagnostics was confirmed standalone at exit 0, because it never executes inside a pnpm verify run that fails at the test step."
  ],
  "limitations": [
    "The workflow has never executed on GitHub. Its first real run occurs when this branch's pull request updates; any syntax or runner-environment defect surfaces there and is not yet proven.",
    "$CLAUDE_PROJECT_DIR expansion in the hook command is unverified on the Founder's Windows workstation. The operations document records the repository-relative fallback if it does not expand.",
    "The pre-flight excludes tests/founder-windows-startup.test.ts and tests/iris-dev-github.test.ts, which require the sibling Command Center workspace and the GitHub CLI. Both fail identically at clean main in the same container, so no regression is hidden, but CI is therefore not full verification and a workstation pnpm verify is still required before merge.",
    "Making those two tests environment-independent would let CI run the complete suite. That is a separate objective and was not authorized here.",
    "The guard covers Claude Code Edit, Write, and NotebookEdit calls only. It does not intercept Codex, shell, or editor edits; CI is the control that catches those.",
    "GitHub Actions consumption is assumed free under public-repository terms consistent with the repository's approved public visibility. No billing state was inspected.",
    "This work is uncertified by the producer: Claude cannot independently certify its own material output. The Founder is the independent reviewer."
  ],
  "rollback": "Delete .github/workflows/verify.yml to remove the pre-flight. Delete the additive hooks key from .claude/settings.json to remove the guard; the script is inert unless registered. To undo the whole objective, history-preservingly revert the commit that introduced these paths. Neither control owns state, creates a resource, or holds a credential, so no destructive cleanup is required.",
  "protected_actions_remaining": [
    "Founder independent review of both controls",
    "First live workflow execution on the pull request, and review of its result",
    "Confirmation of hook behavior on the Founder Windows workstation",
    "pnpm verify on the Founder workstation",
    "Merge to main, provider-equality verification, and branch cleanup under explicit Founder instruction"
  ],
  "ready_for_review": true
}
