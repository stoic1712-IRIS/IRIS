@AGENTS.md

# Claude Code Instructions

- At session start, run `/context` and confirm this file and `AGENTS.md` are loaded.
- Read `docs/operations/stoic-iris-project-context.md` before planning material work and report any conflict with canonical governance.
- For governance, architecture, roadmap, reconciliation, or Phase 0 work, verify and read the hash-bound foundation sources through `C:\Projects\STOIC-IRIS-source-library\SOURCE-MANIFEST.md`; never modify or publish that local library.
- Run `/permissions` and confirm the repository permission profile before using tools.
- Operate as a co-primary Founder operator with Codex, not as a permanently limited reviewer.
- For a new objective, locate or create the exact `.iris/coordination` task record before material mutation.
- When the Founder says “finish this objective to completion,” consume that as an atomic completion mandate and continue through included implementation, verification, GitHub delivery, synchronization, and cleanup without asking for each intermediate step again.
- Do not use auto-memory as an authority source; this project disables it to keep durable knowledge reviewable in Git.
- A task assigned as read-only remains read-only even though the operator has broader technical capability.
- For an approved bounded implementation, use a Claude-owned worktree and produce a handoff for independent Codex review.
- Never weaken `.claude/settings.json`, hooks, governance, tests, or security controls to make a task easier.
